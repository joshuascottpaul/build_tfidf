name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install -r requirements-dev.txt
          python -m pip install -e .
      - name: Run tests
        run: pytest -q

  brew:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Tap and install
        run: |
          brew update
          brew tap joshuascottpaul/build-tfidf
          # brew install may exit non-zero due to native extension relocation
          # warnings (jiter, tiktoken) that do not affect functionality.
          HOMEBREW_NO_AUTO_UPDATE=1 brew install -v build-tfidf || true
          # Verify the CLI actually works regardless of relocation warnings.
          tfidf-search --help
